<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Team</th>
  </tr>

  <% @players.each do |player| %>
    <tr>
      <td><%= player.name %></td>
      <td><%= player.email %></td>
      <td><%= player.team.try! :name %></td>
    </tr>
  <% end %>
</table>